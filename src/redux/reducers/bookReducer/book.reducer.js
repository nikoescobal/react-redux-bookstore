const initialState = {};

export default (state = initialState, action) => {
  switch (action.type) {
    // case typeName:
    //   return { ...state, ...payload };

    default:
      return state;
  }
};
